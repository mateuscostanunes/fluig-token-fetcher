{
  "name": "fluig-token-fetcher",
  "version": "1.0.0",
  "description": "API para capturar JWT token do Fluig automaticamente",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
